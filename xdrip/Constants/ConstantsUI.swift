import Foundation
import UIKit

enum ConstantsUI {
    
    // color for the text in a table view cell, which is not active, or doesn't have effect
    static let colorInActiveSetting = UIColor.gray
    
    // color for the text in a table view cell, which is active
    static let colorActiveSetting = UIColor.black
    
}
